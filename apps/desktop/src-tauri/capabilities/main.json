{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "main-window",
  "description": "P1-A main window only uses core typed commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
